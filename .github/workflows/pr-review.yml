name: PR Security Check

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  ai-security-check:
    permissions:
      pull-requests: write
      contents: read
    uses: ./.github/workflows/ai-security-check.yml
    secrets:
      chat-token: "sk-615a059071774bd7ae4750efc27724a7"